# LoopX

A lightweight, modular, and efficient workflow automation and task‑handling system built with **Node.js**, **Express.js**, **EJS**, and **TailwindCSS**. LoopX focuses on clean architecture, reusable components, and rapid development.

---

## 🚀 Features

* **Modular Express Architecture** (controllers, services, routers, middlewares)
* **EJS View Engine** for clean and dynamic UI rendering
* **TailwindCSS Integration** for fast styling
* **Environment-Based Configuration** using `.env`
* **Reusable Models & Services**
* **Production‑ready folder structure**
* **Easy to scale and maintain**

---

## 📁 Project Structure

```
LoopX/
│
├── Public/               # Static assets (images, fonts, scripts)
├── controller/           # Route controllers (business logic)
├── dist/                 # Compiled / optimized assets
├── middlewares/          # Express middlewares
├── models/               # Database models
├── node_modules/         # Dependencies
├── public/               # Additional static files
├── router/               # Route definitions
├── service/              # Service-layer logic
├── views/                # EJS templates (UI)
│
├── .env                  # Environment variables
├── connect.js            # Database connection
├── index.js              # Main application entry point
├── input.css             # Tailwind input file
├── output.css            # Tailwind output file
├── postcss.config.js     # PostCSS config
├── tailwind.config.js    # Tailwind config
├── package.json
└── package-lock.json
```

---

## ⚙️ Installation

1. Clone the repository:

```bash
git clone https://github.com/Its-Utkarsh1/Loop.git
```

2. Navigate into the folder:

```bash
cd Loop
```

3. Install dependencies:

```bash
npm install
```

4. Create a `.env` file and add necessary environment variables:

```
PORT=3000
DATABASE_URL=your_db_url
```

---

## ▶️ Run the Project

### Development Mode:

```bash
npm run dev
```

### Production Build:

```bash
npm run build
```

### Start Server:

```bash
npm start
```

---

## 🧩 Tech Stack

* **Node.js**
* **Express.js**
* **EJS Template Engine**
* **TailwindCSS**
* **PostCSS**
* **JavaScript (ES6+)**

---

## 📌 Contribution

Contributions are welcome! Follow these steps:

1. Fork the repository
2. Create a new branch (`feature/my-feature`)
3. Commit your changes
4. Push and open a PR

---

 by **Utkarsh Srivastava**
